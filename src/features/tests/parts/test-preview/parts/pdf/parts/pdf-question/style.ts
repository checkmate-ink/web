import { StyleSheet } from "@react-pdf/renderer";

export const styles = StyleSheet.create({
  container: {
    gap: 10,
  },
  questionText: {
    fontFamily: "Work Sans",
    fontSize: 13,
    fontWeight: 500,
    color: "#000000",
  },
});
