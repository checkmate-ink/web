import { StyleSheet } from "@react-pdf/renderer";

export const styles = StyleSheet.create({
  container: {
    gap: 6,
    paddingLeft: 24,
  },
  item: {
    fontFamily: "Work Sans",
    fontSize: 13,
  },
});
