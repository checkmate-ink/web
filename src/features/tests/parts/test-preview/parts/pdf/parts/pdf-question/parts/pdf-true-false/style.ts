import { StyleSheet } from "@react-pdf/renderer";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    gap: 40,
    paddingLeft: 24,
  },
  option: {
    fontFamily: "Work Sans",
    fontSize: 13,
  },
});
